# Legacy Node MCP server

**Superseded by `src/BeeDocs.Mcp` (.NET).**

Windows hosting, `scripts/start.*`, `publish-windows.ps1`, and the K3S MCP
image now use the C# server. This TypeScript package remains for reference /
local experiments only; do not deploy it for new environments.
