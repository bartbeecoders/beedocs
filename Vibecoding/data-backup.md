Add a S3 compatible storage provider.

And add a backup / restore page in the settings section that allows the admin to configure backup/restore of the data to 1 or more storage providers

git remote add origin https://github.com/bartbeecoders/beedocs.git
git branch -u origin/main